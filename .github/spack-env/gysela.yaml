---
spack:
  concretizer:
    reuse: false
    unify: true
  config:
    sandbox:
      enable: true
      allow_network: true
      allow_read:
        - /
      allow_write:
        - /home/runner
        - /dev
  mirrors:
    local-buildcache:
      url: oci://ghcr.io/gyselax/nightly-testing-ubuntu-26.04
      binary: true
      signed: false
      access_pair:
        id_variable: GITHUB_USER
        secret_variable: GITHUB_TOKEN
  packages:
    all:
      require:
        - 'target=x86_64'
      prefer:
        - '~cuda'
        - '~rocm'
        - '~openmp'
        - '~mpi'
        - '~fortran'
    c:
      require: 'gcc@15.2'
    cxx:
      require: 'gcc@15.2'
    fortran:
      require: 'gcc@15.2'
    py-dask:
      prefer:
        - '~dataframe'
    py-pandas:
      prefer:
        - '~performance'
  repos:
    patches:
      git: https://github.com/gyselax/spack.git
      branch: develop
    builtin:
      branch: develop
  specs:
    - 'gysela-mini-app-io'
  view:
    default:
      projections:
        all: "{name}-{version}-{hash}"
      root: .spack-env/view
